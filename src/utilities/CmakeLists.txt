# here we create the PlaneLibrary
add_library(UtilitiesLibrary InfoRetriever.hpp InfoRetriever.cpp)

# here we specify the include directories for PlaneLibrary
target_include_directories(UtilitiesLibrary PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})